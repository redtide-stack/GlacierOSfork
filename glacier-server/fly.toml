# fly.toml app configuration file generated for glacier on 2024-08-01T22:59:58Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "glacier"
primary_region = "lax"

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[tool.uv]
override-dependencies = [
  "banned-package-name; sys_platform == 'never'",
]
